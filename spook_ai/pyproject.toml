[project]
name = "spook_ai"
dynamic = ["version"]
requires-python = ">=3.10"
dependencies = [
    "pydantic-ai==1.56.0"
]
readme = "README.md"

[project.scripts]
spook_ai = "spook_ai.__main__:main"


[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"