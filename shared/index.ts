/**
 * Shared types module for VSCode extension and webview communication.
 * 
 * Usage in extension (src/):
 *   import { Message, ServerStatus } from './model';
 * 
 * Usage in webview:
 *   import { Message, ServerStatus } from '../shared/types';
 */
export * from './types';
